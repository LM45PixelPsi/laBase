// const psi = 1.3247;

// console.log(psi ** 4);

const psi = "1.3247";
document.documentElement.style.setProperty("--psi", psi);

const psi2 = Math.pow(psi, 2);
const psi2p = 100 / psi2 + "%";
document.documentElement.style.setProperty("--psi2p", psi2p);

// ----------------------------

const psi0 = Math.pow(psi, 0);
const p0 = 100 / psi0 + "%";

const psi1 = Math.pow(psi, 1);
const p1 = 100 / psi1 + "%";

const p2 = 100 / psi2 + "%";

const psi3 = Math.pow(psi, 3);
const p3 = 100 / psi3 + "%";

const psi4 = Math.pow(psi, 4);
const p4 = 100 / psi4 + "%";

const psi5 = Math.pow(psi, 5);
const p5 = 100 / psi5 + "%";

const psi6 = Math.pow(psi, 6);
const p6 = 100 / psi6 + "%";

const psi7 = Math.pow(psi, 7);
const p7 = 100 / psi7 + "%";

const psi8 = Math.pow(psi, 8);
const p8 = 100 / psi8 + "%";

const psi9 = Math.pow(psi, 9);
const p9 = 100 / psi9 + "%";

const psi10 = Math.pow(psi, 10);
const p10 = 100 / psi10 + "%";

const psi11 = Math.pow(psi, 11);
const p11 = 100 / psi11 + "%";

const psi12 = Math.pow(psi, 12);
const p12 = 100 / psi12 + "%";

const psi13 = Math.pow(psi, 13);
const p13 = 100 / psi13 + "%";

const psi14 = Math.pow(psi, 14);
const p14 = 100 / psi14 + "%";

const psi15 = Math.pow(psi, 15);
const p15 = 100 / psi15 + "%";

const psi16 = Math.pow(psi, 16);
const p16 = 100 / psi16 + "%";

const psi17 = Math.pow(psi, 17);
const p17 = 100 / psi17 + "%";

const psi18 = Math.pow(psi, 18);
const p18 = 100 / psi18 + "%";

const psi19 = Math.pow(psi, 19);
const p19 = 100 / psi19 + "%";

const psi20 = Math.pow(psi, 20);
const p20 = 100 / psi20 + "%";

const psi21 = Math.pow(psi, 21);
const p21 = 100 / psi21 + "%";

const psi22 = Math.pow(psi, 22);
const p22 = 100 / psi22 + "%";

console.log(
  p0,
  p1,
  p2,
  p3,
  p4,
  p5,
  p6,
  p7,
  p8,
  p9,
  p10,
  p11,
  p12,
  p13,
  p18,
  p22
);
